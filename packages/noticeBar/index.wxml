<wxs src="../wxs/utils.wxs" module="utils" />
<view 
    class="jmd__noticeBarContainer hover-class"
    wx:if="{{ show }}"
    style="color: {{ color }}; background-color: {{ backgroundColor }};"
    bind:tap="onClick">
   <view
      wx:if="{{ showIcon }}"
      class=" {{ utils.bem('noticeBarContainer',['iconLeft'])}} ">
      icon
   </view>

   <view 
      class="{{ utils.bem('noticeBarContainer',['textWrapper'])}}"
       style="height:{{ wrapabled ? 'auto' : '24px' }}">

      <view 
         class="{{ utils.bem('noticeBarContainer',['text'])}} " 
         animation="{{ scrollable ? animation : '' }}" 
         style="left:{{ scrollable ? '100%':'0' }}; position:{{ scrollable ? 'absolute':'relative' }};white-space:{{ wrapabled ? 'wrap' : 'nowrap' }}"
         bindtransitionend="transitionEnd">
         {{ text }}
      </view>

   </view>

   <view 
      wx:if="{{ closeable || mode == 'info'}}"
      class="{{ utils.bem('noticeBarContainer',['iconRight'])}} "
      bind:tap="closeNotice">
      x
   </view>
     <navigator
      wx:if="{{ mode === 'link' }}"
      class="{{ utils.bem('noticeBarContainer',['iconRight'])}} "
      url="{{ url }}">
      >
  </navigator>
</view>

 