<be-transition
  name="fade"
  show="{{ show }}"
  custom-class="be-toast__container"
  custom-style="z-index: {{ zIndex }};"
>
  <view
    class="be-toast custom-class be-toast--{{position}} {{ (iconName || customIcon) ? 'be-toast--with-icon' : '' }} {{ iconName === 'loading' && !msg ? 'be-toast--loading' : '' }}"
  >
    <!--iconName优先级更高-->
    <be-loading
      jd:if="{{iconName === 'loading'}}"
      type="{{ loadingType }}"
      custom-class="{{ msg ? 'be-toast__loading' : '' }}"
    />
    <be-icon jd:elif="{{iconName}}" class="be-toast__icon" name="{{iconName}}"/>
    <view jd:elif="{{customIcon}}" class="be-toast__icon custom-icon-class"/>
    <!--文本-->
    <view jd:if="{{ msg }}" class="be-toast__msg">{{ msg }}</view>
  </view>
</be-transition>
