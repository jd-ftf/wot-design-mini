import VueComponent from '../common/component'

VueComponent({
  props: {
    iconName: String,
    iconClass: String,
    msg: String,
    position: String,
    show: Boolean,
    zIndex: Number
  }
})