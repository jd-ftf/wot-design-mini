<view class="jm-progress {{ hideText ? '' : 'is-with-text' }} custom-class">
  <!--文案-->
  <view jd:if="{{!hideText}}" class="jm-progress__label" style="left: {{percentage}}%">{{ percentage }}%</view>
  <!--进度条-->
  <view class="jm-progress__outer">
    <view
      class="jm-progress__inner"
      style="background:{{showColor}}; width:{{showPercent}}%;transition-duration:{{changeCount * duration * 0.001}}s;"
    >
    </view>
  </view>
</view>