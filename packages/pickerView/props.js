export default {
  // 加载中
  loading: Boolean,
  // 选项总高度
  columnsHeight: {
    type: Number,
    value: 231
  },
  // 选项对象中，value对应的 key
  valueKey: {
    type: String,
    value: 'value'
  },
  // 选项对象中，展示的文本对应的 key
  labelKey: {
    type: String,
    value: 'label'
  }
}