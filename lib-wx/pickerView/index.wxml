<view class="jm-picker-view">
  <view
    class="jm-picker-view__loading"
    wx:if="{{loading}}"
  >
    <jm-loading color="#0083FF" size="33px"/>
  </view>
  <picker-view
    mask-class="jm-picker-view__mask"
    indicator-class=""
    indicator-style="height: {{itemHeight}}px;"
    style="height: {{itemHeight * visibleItemCount}}px;"
    value="{{selectedIndex}}"
    bindchange="handleChange"
  >
    <picker-view-column
      wx:for="{{formatColumns}}"
      wx:for-index="colIndex"
      wx:for-item="col"
      wx:key="$this"
      class="jm-picker-view-column"
    >
      <view
        wx:for="{{col}}"
        wx:for-index="rowIndex"
        wx:for-item="row"
        wx:key="$this"
        class="jm-picker-view-column__item {{row['disabled']?'jm-picker-view-column__item--disabled':''}} {{selectedIndex[colIndex] == rowIndex ? 'jm-picker-view-column__item--active' : ''}}"
        style="line-height: {{itemHeight}}px;"
      >
        {{row[labelKey]}}
      </view>
    </picker-view-column>
  </picker-view>
</view>
