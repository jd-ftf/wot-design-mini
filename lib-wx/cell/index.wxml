<view class="jm-cell custom-class {{noHair?'is-no-hair':'' }}"
      hover-class="{{clickable?'is-hover':'none'}}"
      hover-stay-time="70"
      bindtap="onClick">
  <!--左侧icon部位-->
  <jm-icon
    wx:if="{{icon}}"
    name="{{icon}}"
    class="jm-cell__icon custom-icon-class"
  >
  </jm-icon>
  <view wx:else class="custom-icon-class">
    <slot name="icon"/>
  </view>

  <view class="jm-cell__wrapper">
    <view class="jm-cell__title">
      <!--title BEGIN-->
      <view class="custom-title-class">
        <block wx:if="{{title}}">{{title}}</block>
        <slot wx:else name="title"></slot>
      </view>
      <!--title END-->

      <!--label BEGIN-->
      <view wx:if="{{label}}" class="jm-cell__label custom-label-class">
        <block>{{label}}</block>
      </view>
      <slot wx:else name="label"/>
      <!--label END-->
    </view>

    <!--right content BEGIN-->
    <view class="jm-cell__right">
      <!--文案内容-->
      <view class="jm-cell__value custom-value-class">
        <block wx:if="{{value}}">{{value}}</block>
        <slot wx:else></slot>
      </view>
      <!--箭头-->
      <jm-icon wx:if="{{isLink}}" class="jm-cell__arrow-right" name="arrow-right"/>
    </view>
    <!--right content END-->
  </view>
</view>