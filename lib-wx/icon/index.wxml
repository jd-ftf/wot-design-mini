<view class="be-icon custom-class {{isImageUrl ? 'be-icon--image' : ('be-icon-' + name)}}"
      style="color: {{color}}; font-size: {{size}}">
  <image wx:if="{{isImageUrl}}" class="be-icon__image" src="{{name}}"></image>
</view>