<view class="jm-icon custom-class {{isImageUrl ? 'jm-icon--image' : ('jm-icon-' + name)}}"
      style="color: {{color}}; font-size: {{size}}">
  <image wx:if="{{isImageUrl}}" class="jm-icon__image" src="{{name}}"></image>
</view>