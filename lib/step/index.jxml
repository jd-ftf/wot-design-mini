<view
  jd:if="{{ currentStatus }}"
  class="jm-step custom-class {{ currentStatus ? 'is-' + currentStatus : '' }} {{ canAlignCenter ? 'is-center' : '' }} {{ vertical ? 'is-vertical' : '' }}"
  style="{{ styles }}"
>
  <view class="jm-step__header {{ dot ? 'is-dot' : '' }}">
    <view class="jm-step__icon {{ dot ? 'is-dot' : (!!icon || iconSlot ? 'is-icon' : 'is-text') }}">
      <view jd:if="{{ dot }}" class="jm-step__dot"></view>
      <slot jd:elif="{{ iconSlot }}" name="icon"/>
      <jm-icon jd:elif="{{ icon }}" class="jm-step__icon-inner" name="{{ icon }}"/>
      <view
        jd:elif="{{ currentStatus !== 'finished' && currentStatus !== 'error' }}"
        class="jm-step__icon-outer"
      >
        {{ index + 1 }}
      </view>
      <jm-icon
        jd:else
        class="jm-step__icon-inner"
        name="{{ currentStatus === 'finished' ? 'success' : 'close' }}"
      />
    </view>
    <view jd:if="{{ index < childrenLength - 1 }}" class="jm-step__line"></view>
  </view>
  <view class="jm-step__content">
    <view class="jm-step__title {{ descriptionSlot || description ? 'is-description' : '' }}">
      <slot jd:if="{{ titleSlot }}" name="title"/>
      <text jd:else>{{ currentTitle }}</text>
    </view>
    <view jd:if="{{ descriptionSlot || description }}" class="jm-step__description">
      <slot jd:if="{{ descriptionSlot }}"/>
      <text jd:else>{{ description }}</text>
    </view>
  </view>
</view>