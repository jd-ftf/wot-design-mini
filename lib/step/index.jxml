<view
  jd:if="{{ currentStatus }}"
  class="be-step custom-class {{ currentStatus ? 'is-' + currentStatus : '' }} {{ canAlignCenter ? 'is-center' : '' }} {{ vertical ? 'is-vertical' : '' }}"
  style="{{ styles }}"
>
  <view class="be-step__header {{ dot ? 'is-dot' : '' }}">
    <view class="be-step__icon {{ dot ? 'is-dot' : (!!icon || iconSlot ? 'is-icon' : 'is-text') }}">
      <view jd:if="{{ dot }}" class="be-step__dot"></view>
      <slot jd:elif="{{ iconSlot }}" name="icon"/>
      <be-icon jd:elif="{{ icon }}" class="be-step__icon-inner" name="{{ icon }}"/>
      <view
        jd:elif="{{ currentStatus !== 'finished' && currentStatus !== 'error' }}"
        class="be-step__icon-outer"
      >
        {{ index + 1 }}
      </view>
      <be-icon
        jd:else
        class="be-step__icon-inner"
        name="{{ currentStatus === 'finished' ? 'success' : 'close' }}"
      />
    </view>
    <view jd:if="{{ index < childrenLength - 1 }}" class="be-step__line"></view>
  </view>
  <view class="be-step__content">
    <view class="be-step__title {{ descriptionSlot || description ? 'is-description' : '' }}">
      <slot jd:if="{{ titleSlot }}" name="title"/>
      <text jd:else>{{ currentTitle }}</text>
    </view>
    <view jd:if="{{ descriptionSlot || description }}" class="be-step__description">
      <slot jd:if="{{ descriptionSlot }}"/>
      <text jd:else>{{ description }}</text>
    </view>
  </view>
</view>