<demo-block transparent>
  <be-picker label="单列选项" columns="{{columns0}}"/>

  <be-picker label="禁用" disabled="{{true}}" value="{{value1}}" columns="{{columns1}}"/>

  <be-picker label="只读" readonly value="{{value2}}" columns="{{columns2}}"/>

  <be-picker label="loading" loading="{{true}}" columns="{{columns3}}"/>

  <be-picker label="多列" value="{{value4}}" columns="{{columns4}}"/>

  <be-picker
    label="多级联动"
    value="{{value5}}"
    columns="{{columns5}}"
    column-change="{{onChangeDistrict}}"
  />

  <be-picker
    label="分隔符"
    value="{{value6}}"
    columns="{{columns6}}"
    display-format="{{displayFormat}}"
  />

  <be-picker label="标题" columns="{{columns7}}" title="文案标题"/>

</demo-block>

